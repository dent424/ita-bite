<body>
	<div class="row">
		<div class="span7 offset4">
			<legend><%=t :ingredient_edit %></legend>
		</div>
	</div>

	<div class="row-fluid">
		<div class="span6 offset3">
			<%= form_for([@item, @round], :html=> {:class => 'form-horizontal'}) do |f| %>

			<div class="control-group">
				<label class="control-label">
					<%=t :ingredient_material %>
				</label>
				<div class="controls">
					<%= f.text_field :material %>
				</div>
			</div>

			<div class="control-group">
				<label class="control-label">
					<%=t :ingredient_length %>
				</label>
				<div class="controls">
					<%= f.text_field :length %>
				</div>
			</div>

			<div class="control-group">
				<label class="control-label">
					<%=t :ingredient_diameter %>
				</label>
				<div class="controls">
					<%= f.text_field :diameter %>
				</div>
			</div>

			<div class="control-group">
				<label class="control-label">
					<%=t :pieces %>
				</label>
				<div class="controls">
					<%= f.text_field :pieces %>
				</div>
			</div>

			<div class="control-group">
				<label class="control-label">
					<%=t :ordered %>
				</label>
				<div class="controls">
					<%= f.date_select :order_date, :start_year => Time.now.year - 1, :end_year => Time.now.year, :order => [:year, :month, :day]%>
				</div>
			</div>
			
			<div class="control-group">
				<label class="control-label">
					<%=t :delivered %>
				</label>
				<div class="controls">
					<%= f.date_select :delivery_date, :start_year => Time.now.year - 1, :end_year => Time.now.year, :order => [:year, :month, :day], :include_blank => true %>
				</div>
			</div>

			<div class="control-group">
				<div class="controls">
					<%= f.submit "Submit", :class => "btn" %>
				</div>
			</div>
		</div>
	<div>
</body>

<% end %>