<div class="row">
	<div class="span6 offset1">
		<h1><%= @machine.name %></h1>
		<h3><%= @machine.machine_type %></h3>
		<p><%= @machine.notes %></p>
		<%= link_to "Edit Machine",  edit_machine_path(@machine), {:class => 'btn btn-primary'} %>
		<%= link_to " Delete Item",machine_path(@machine), {:confirm => 'Are you sure?', :class => 'btn btn-danger', :method => :delete}  %>
	</div>
</div>

<% if @machine.employees.exists? %>
	<div class="row">
		<div class="span6 offset1">
			<table class="table">
				<thead>
					<tr>
						<th>Name</th>
						<th>Action</th>
					</tr>
				</thead>
				<tbody>
					<% @machine.employees.each do |employee| %>
					<tr>
						<td><%= link_to employee.last_name + employee.first_name, employee %></td>
						<td><%= link_to "Remove",employee_machine_approval_path(@machine, employee), {:confirm => 'Are you sure?', :method => :delete}  %></td>
					</tr>
					<% end %>	
				</tbody>
			</table>
		</div>
	</div>
<% end %>